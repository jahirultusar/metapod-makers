

--- 

## Ticket: Create S3 bucket to hold Terraform state
    
    Important:
        - Make sure you create the bucket in the eu-west-2 region
        - Make sure you enable bucket versioning.

Step 1: Create the S3 bucket

    Using AWS CLI:
    aws s3api create-bucket \
    --bucket jay-metapod--tf-state \
    --region eu-west-2 \
    --create-bucket-configuration Locati

Step 2: Enable versioning

    aws s3api put-bucket-versioning \
    --bucket jay-metapod-tf-state \
    --versioning-configuration Status=Enabled

Step 3: Verify

    aws s3api get-bucket-versioning \
    --bucket jay-metapod-tf-state

---

## Ticket: Using Terraform with GitHub Actions

Addded env variables and secrets to tf git workflows

    env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_DEFAULT_REGION: eu-west-2

--- 

## Ticket: Provisioning EKS Cluster
    # Added this to find /tf-files in child module
        module "eks_stack" {
        source = "./tf-files"

        cluster_name = "jay-metapod-eks-cluster"
}